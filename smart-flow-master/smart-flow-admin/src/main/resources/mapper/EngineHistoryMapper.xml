<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.smartboot.smart.flow.admin.mapper.EngineHistoryMapper">

    <resultMap id="BaseResultMap" type="org.smartboot.smart.flow.admin.model.EngineHistory">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="engine_name" property="engineName" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="BLOB"/>
        <result column="engine_id" property="engineId" jdbcType="BIGINT"/>
        <result column="version" property="version" jdbcType="VARCHAR"/>
        <result column="created" property="created" jdbcType="TIMESTAMP"/>
        <result column="updated" property="updated" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="queryHistory" resultMap="BaseResultMap">
      select id, engine_name, engine_id, version, created, updated
      from engine_table_history
      where engine_id = #{engineId}
      order by id desc;
    </select>

    <select id="detail" resultMap="BaseResultMap">
        select *
        from engine_table_history
        where id = #{id}
    </select>

    <insert id="insert" parameterType="org.smartboot.smart.flow.admin.model.EngineHistory">
        insert into engine_table_history(engine_id, engine_name, content, version, created, updated)
        values(#{config.engineId}, #{config.engineName}, #{config.content}, #{config.version}, now(), now())
    </insert>

</mapper>